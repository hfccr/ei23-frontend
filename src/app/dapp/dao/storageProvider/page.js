"use client";
import { ProviderWhitelist } from "@/app/components/ProviderWhitelist";

// Check if user is registered
// If registered, show yes
// Else show registration form

export default function Client() {
  return <ProviderWhitelist />;
}
